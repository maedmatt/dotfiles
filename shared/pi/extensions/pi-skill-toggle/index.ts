// Entry point lives here rather than in src/ so pi labels the extension
// "pi-skill-toggle" instead of "src" (it names extensions after the directory
// holding index.ts).
export { default } from "./src/index.ts";
